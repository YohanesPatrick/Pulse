# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Pulse2' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Pulse2
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'
  pod 'Firebase/Storage'
  pod 'ResearchKit', '~> 1.0'
  pod 'SnapKit'
  pod 'Charts'

  target 'Pulse2Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Pulse2UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
